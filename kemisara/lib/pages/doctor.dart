import 'package:flutter/material.dart';

class DoctorAppointmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Doctor's Appointment")),
      body: Center(child: Text("This is the Doctor's Appointment Page")),
    );
  }
}